package dataStructure.test;import dataStructure.LinkedList;import dataStructure.ListI;import java.util.Iterator;public class Tester {    public static void main(String[] args) {        ListI<Integer> list = new LinkedList<Integer>();        int n = 10;        // 링크드 리스트 0 ~ 9까지 각 요소 초기화        for (int i = 0; i < n; i++) {            list.addFirst(i);        }        // 반복자 테스트        Iterator<Integer> helper = list.iterator();        while (helper.hasNext()) {            System.out.println(helper.next());        }        // 링크드 리스트 내부 요소를 삭제        for (int i = n - 1; i >= 0; i--) {            int x = list.removeFirst();        }    }}